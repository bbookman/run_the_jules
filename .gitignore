# Node.js
node_modules/
npm-debug.log
yarn-error.log
# yarn.lock # Usually committed, but can be ignored if desired
# package-lock.json # Usually committed, but can be ignored if desired
!package.json

# Logs
logs/
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
lcov.info
*.swp

# Environment variables
.env
.env.*
!/.env.template
!/.env.example # Common practice for example env files

# Build output
build/
dist/
out/
coverage/
*.tgz

# OS generated files
.DS_Store
.DS_Store?
._*
.Spotlight-V100
.Trashes
ehthumbs.db
Thumbs.db

# Editor directories and files
.idea/
.vscode/
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw? # Vim swap files

# Docker
docker-compose.override.yml

# Frontend specific (if frontend is in a subfolder like src/frontend)
src/frontend/node_modules/
src/frontend/build/
src/frontend/coverage/
src/frontend/.env
src/frontend/.env.*
!src/frontend/.env.template
!src/frontend/.env.example

# If logs are at root and meant to be an empty tracked dir
# /logs/*
# !/logs/.gitkeep # To keep the 'logs' directory itself tracked

# Explicitly ignore log files in the root logs directory
# This ensures that even if the 'logs' directory is tracked (e.g. via .gitkeep),
# the log files themselves are ignored.
/logs/*.log
/logs/error.log
/logs/combined.log

# Python cache files (if any Python scripts are ever used for utilities, good to keep)
__pycache__/
*.py[cod]
*$py.class